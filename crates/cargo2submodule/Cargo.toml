[package]
name = "cargo2submodule"
version = "0.1.0"
edition = "2024"

[dependencies]

cargo_metadata = { path = "../../vendor/cargo_metadata" }
clap = { version = "4.5.4", features = ["derive"] }
serde = { version = "1.0.197", features = ["derive"] }
