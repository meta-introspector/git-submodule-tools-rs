## `split_current_tmux_window.sh`

**Summary:**
This simple script splits the current tmux window into two panes. By default, it creates a vertical split. It is intended to be run from within an active tmux session. After execution, it informs the user that the window has been split and provides a tip for navigating between the new panes.
