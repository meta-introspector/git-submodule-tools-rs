[package]
name = "poem_analyzer"
version = "0.1.0"
edition = "2024"

[dependencies]
walkdir = "2.5.0"
regex = "1.10.0"
git2 = "0.19.0"
anyhow = "1.0"
chrono = { version = "0.4", features = ["clock", "std"] }

[lints]
workspace = true
